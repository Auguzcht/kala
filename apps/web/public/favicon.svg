<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32">
  <!-- The real hornbill mark (public/assets/kala-mark.png), referenced rather
       than re-drawn: there is no vector source for it yet (see the note in
       src/components/brand/KalaMark.tsx). Browsers render an <image> inside
       an SVG favicon fine, so this replaces the old two-chevron placeholder
       with the actual brand mark instead of leaving a TODO in place. -->
  <image href="/assets/kala-mark.png" width="32" height="32" />
</svg>
