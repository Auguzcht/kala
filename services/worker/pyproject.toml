[project]
name = "kala-worker"
version = "0.1.0"
description = "Kala async worker: twin recompute + re-embed, EventBridge-scheduled"
requires-python = ">=3.12"
dependencies = [
    "boto3>=1.34",
    "httpx>=0.27",
]

[dependency-groups]
dev = [
    "pytest>=8.3",
    "ruff>=0.7",
]

[tool.uv]
package = false
